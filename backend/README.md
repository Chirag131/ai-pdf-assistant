# Backend

Python backend for the AI PDF Assistant.

## Current functionality

- Opens a local PDF
- Extracts selectable text page by page
- Preserves page numbers
- Displays extracted text in the terminal
- Handles missing and invalid PDF files

## Setup

Create a virtual environment:

```bash
python3 -m venv .venv